import User from '../user';

const Home = async () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center">
      <User />
    </main>
  );
};

export default Home;
