export { default } from 'next-auth/middleware';

export const config = {
  matcher: [
    // '/dashboard/:path*',
    // '/add-car/:path*',
    // '/user/:path*',
    // '/api/cars/:path*',
    // '/api/users/:path*',
    // '/editor/:path*',
    // '/login',
    // '/register',
  ],
};
